import absLogoIcon from '../assets/abs-logo-icon.png';

/** Default ABS product logo (sidebar, auth, receipts fallback). Source: repo root `ABS Ghana.png`; run `scripts/sync-abs-logo.sh` after updating. */
export const APP_LOGO_SRC = absLogoIcon;

export const APP_LOGO_ALT = 'ABS';
