/**
 * Business types that use studio-like workflows (jobs, quotes, deliveries, etc.).
 * @type {readonly string[]}
 */
export const STUDIO_LIKE_TYPES = Object.freeze([
  'printing_press',
  'mechanic',
  'barber',
  'salon',
  'studio',
]);
